name: test

on:
  push:
    branches: [main]


jobs:
  dependencies:
    runs-on: windows-latest
    name: dependencies install
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.11'
      - name: dependencies
        run: npm i
  build:
    runs-on: windows-latest
    needs: dependencies
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.11'
      - name: build
        run: npm run build
  linters:
    runs-on: windows-latest
    needs: dependencies
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.11'
      - name: linters
        run: npm run linters
  deploy:
    runs-on: windows-latest
    needs: [build, linters]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.11'
      - name: deploy
        run: npm run deploy
